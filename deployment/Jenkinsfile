pipeline {

    agent any

    environment {

        AWS_REGION = 'ap-south-1'

        PROJECT_NAME = 'sfa'

        AWS_ACCESS_KEY_ID = credentials('aws-access-key-id')

        AWS_SECRET_ACCESS_KEY = credentials('aws-secret-access-key')
        
        AWS_ACCOUNT_ID = credentials('aws-rahul-account-id')

        CODEBUILD_PROJECT_NAME = 'sfa-build'

        FRONTEND_ECR_REPOSITORY = 'sfa-frontend'
        BACKEND_ECR_REPOSITORY  = 'sfa-backend'

        ECS_CLUSTER = 'its360-cluster'

        GITHUB_PAT = credentials('hrms-git-token')

        GIT_REPO = "https://${GITHUB_PAT}@github.com/IT-SOFTLAB/SFA-New.git"

        BACKEND_PRODUCTION_ENV_S3_KEY = 'sfa-backend-production-env'
        FRONTEND_PRODUCTION_ENV_S3_KEY = 'sfa-frontend-production-env'
        BACKEND_STAGING_ENV_S3_KEY = 'sfa-backend-staging-env'
        FRONTEND_STAGING_ENV_S3_KEY = 'sfa-frontend-staging-env'

    }

    stages {

        stage('Validate Git Tag') {
            steps {
                script {
                    // Use tag(s) pointing at HEAD to avoid picking an unrelated latest tag
                    env.TAG_NAME = sh(script: 'git tag --points-at HEAD | sort -V | tail -n 1 || echo ""', returnStdout: true).trim()

                    if (!env.TAG_NAME) {
                        error '❌ No tag detected! Deployment only triggers on tag creation.'
                    }
                    echo "📌 Detected Tag: ${env.TAG_NAME}"

                    // Extract branch from which the tag originated.
                    env.BRANCH = sh(script: "git branch -r --contains ${env.TAG_NAME} | grep -Eo 'origin/(main|staging)' | head -n 1 | cut -d '/' -f2 || echo ''", returnStdout: true).trim()
                    if (!env.BRANCH) {
                        error "❌ Invalid tag! Tags must be from 'main', 'staging' branches."
                    }

                    if (env.BRANCH == 'main' && env.TAG_NAME ==~ /^v\d+\.\d+\.\d+$/) {
                        env.ENVIRONMENT = 'production'
                    } else if (env.BRANCH == 'staging' && env.TAG_NAME ==~ /^v\d+\.\d+\.\d+-staging$/) {
                        env.ENVIRONMENT = 'staging'
                    } else {
                        error "❌ Invalid tag format for branch '${env.BRANCH}'."
                    }
                    echo "🌍 Deployment Environment: ${env.ENVIRONMENT}"
                    echo "Project: ${PROJECT_NAME}"
                    echo "Branch: ${env.BRANCH}"
                    echo "Tag: ${env.TAG_NAME}"
                }
            }
        }

        stage('Create Docker Tags') {
            steps {
                script {
                    env.BACKEND_TAG = "backend-${env.ENVIRONMENT}-${env.TAG_NAME}"
                    env.FRONTEND_TAG = "frontend-${env.ENVIRONMENT}-${env.TAG_NAME}"
                    echo "🛠️ Backend Tag: ${env.BACKEND_TAG}"
                    echo "🛠️ Frontend Tag: ${env.FRONTEND_TAG}"
                }
            }
        }


        stage('Determine secret key file') {
            steps {
                script {
                    // -------------------------------------------------
                    // Make sure environment was determined correctly
                    // -------------------------------------------------
                    if (!env.ENVIRONMENT) {
                        error """
                        ❌ Secret key determination failed.

                        ENVIRONMENT is not available.

                        The 'Validate Git Tag' stage must complete
                        successfully before this stage.
                        """
                    }

                    if (env.ENVIRONMENT == 'production') {

                        env.BACKEND_SECRET_KEY = "${env.BACKEND_PRODUCTION_ENV_S3_KEY}.env"

                        env.FRONTEND_SECRET_KEY = "${env.FRONTEND_PRODUCTION_ENV_S3_KEY}.env"
                    }

                    else if (env.ENVIRONMENT == 'staging') {

                        env.BACKEND_SECRET_KEY = "${env.BACKEND_STAGING_ENV_S3_KEY}.env"

                        env.FRONTEND_SECRET_KEY = "${env.FRONTEND_STAGING_ENV_S3_KEY}.env"
                    }

                    else {
                        error "❌ Unknown deployment environment: ${env.ENVIRONMENT}"
                    }

                    echo "🔐 Backend env file: ${env.BACKEND_SECRET_KEY}"
                    echo "🔐 Frontend env file: ${env.FRONTEND_SECRET_KEY}"
                }
            }
        }


        stage('Check and Trigger AWS CodeBuild') {
            steps {
                script {

                    echo "============================================"
                    echo "AWS CodeBuild Validation & Trigger"
                    echo "============================================"
                    echo "Project     : ${PROJECT_NAME}"
                    echo "Environment : ${ENVIRONMENT}"
                    echo "AWS Region  : ${AWS_REGION}"
                    echo "CodeBuild   : ${CODEBUILD_PROJECT_NAME}"
                    echo "============================================"

                    // -------------------------------------------------
                    // Check CodeBuild project
                    // -------------------------------------------------

                    def codeBuildStatus = sh(
                        script: """
                            aws codebuild batch-get-projects \
                                --names "${CODEBUILD_PROJECT_NAME}" \
                                --region "${AWS_REGION}" \
                                --query 'projects[0].name' \
                                --output text
                        """,
                        returnStdout: true
                    ).trim()

                    if (!codeBuildStatus ||
                        codeBuildStatus == 'None' ||
                        codeBuildStatus == 'null') {

                        error """
                        ❌ AWS CodeBuild project not found.

                        Project     : ${CODEBUILD_PROJECT_NAME}
                        Environment : ${ENVIRONMENT}
                        AWS Region  : ${AWS_REGION}
                        AWS Account : ${AWS_ACCOUNT_ID}
                        """
                    }

                    echo "✅ CodeBuild project exists: ${codeBuildStatus}"

                    // -------------------------------------------------
                    // Check whether the exact images already exist
                    // -------------------------------------------------

                    def backendExists = sh(
                        script: """
                            aws ecr describe-images \
                                --repository-name ${BACKEND_ECR_REPOSITORY} \
                                --image-ids imageTag=${BACKEND_TAG} \
                                --region ${AWS_REGION} \
                                >/dev/null 2>&1
                        """,
                        returnStatus: true
                    ) == 0

                    def frontendExists = sh(
                        script: """
                            aws ecr describe-images \
                                --repository-name ${FRONTEND_ECR_REPOSITORY} \
                                --image-ids imageTag=${FRONTEND_TAG} \
                                --region ${AWS_REGION} \
                                >/dev/null 2>&1
                        """,
                        returnStatus: true
                    ) == 0

                    env.ECR_REGISTRY = "${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com"

                    env.FRONTEND_IMAGE = "${env.ECR_REGISTRY}/${FRONTEND_ECR_REPOSITORY}:${env.FRONTEND_TAG}"
                    env.BACKEND_IMAGE  = "${env.ECR_REGISTRY}/${BACKEND_ECR_REPOSITORY}:${env.BACKEND_TAG}"

                    if (backendExists && frontendExists) {

                        env.CODEBUILD_BUILD_ID = 'SKIPPED'

                        echo "✅ Both exact images already exist in ECR."
                        echo "Frontend: ${env.FRONTEND_IMAGE}"
                        echo "Backend : ${env.BACKEND_IMAGE}"
                        echo "⏭️ Skipping CodeBuild."

                    } else {

                        echo "🚀 Starting AWS CodeBuild..."
                        echo "Frontend image exists: ${frontendExists}"
                        echo "Backend image exists : ${backendExists}"

                        def buildId = sh(
                            script: """
                                aws codebuild start-build \
                                    --project-name "${CODEBUILD_PROJECT_NAME}" \
                                    --source-version "${TAG_NAME}" \
                                    --region "${AWS_REGION}" \
                                    --environment-variables-override \
                                        name=ENVIRONMENT,value=${ENVIRONMENT},type=PLAINTEXT \
                                        name=PROJECT_NAME,value=${PROJECT_NAME},type=PLAINTEXT \
                                        name=GIT_TAG,value=${TAG_NAME},type=PLAINTEXT \
                                        name=BACKEND_TAG,value=${BACKEND_TAG},type=PLAINTEXT \
                                        name=FRONTEND_TAG,value=${FRONTEND_TAG},type=PLAINTEXT \
                                        name=BACKEND_SECRET_KEY,value=${BACKEND_SECRET_KEY},type=PLAINTEXT \
                                        name=FRONTEND_SECRET_KEY,value=${FRONTEND_SECRET_KEY},type=PLAINTEXT \
                                    --query 'build.id' \
                                    --output text
                            """,
                            returnStdout: true
                        ).trim()

                        if (!buildId ||
                            buildId == 'None' ||
                            buildId == 'null') {

                            error "❌ Failed to start AWS CodeBuild."
                        }

                        env.CODEBUILD_BUILD_ID = buildId

                        echo "✅ AWS CodeBuild started."
                        echo "Build ID: ${env.CODEBUILD_BUILD_ID}"
                    }

                    echo "============================================"
                    echo "CodeBuild Result"
                    echo "============================================"
                    echo "Build ID      : ${env.CODEBUILD_BUILD_ID}"
                    echo "Frontend Image: ${env.FRONTEND_IMAGE}"
                    echo "Backend Image : ${env.BACKEND_IMAGE}"
                    echo "============================================"
                }
            }
        }


        stage('Wait for AWS CodeBuild Completion') {
            when {
                expression {
                    env.CODEBUILD_BUILD_ID && env.CODEBUILD_BUILD_ID != 'SKIPPED'
                }
            }
            steps {
                script {

                    echo "⏳ Waiting for AWS CodeBuild to complete..."

                    timeout(time: 30, unit: 'MINUTES') {

                        def buildStatus = ''

                        while (true) {

                            buildStatus = sh(
                                script: """
                                    aws codebuild batch-get-builds \
                                        --ids ${CODEBUILD_BUILD_ID} \
                                        --region ${AWS_REGION} \
                                        --query 'builds[0].buildStatus' \
                                        --output text
                                """,
                                returnStdout: true
                            ).trim()

                            echo "Build status: ${buildStatus}"

                            if ([
                                'SUCCEEDED',
                                'FAILED',
                                'STOPPED',
                                'FAULT',
                                'TIMED_OUT'
                            ].contains(buildStatus)) {
                                break
                            }

                            sleep 10
                        }

                        if (buildStatus != 'SUCCEEDED') {
                            error "❌ AWS CodeBuild failed with status: ${buildStatus}"
                        }

                        echo "✅ AWS CodeBuild completed successfully."
                    }
                }
            }
        }


        stage('Verify ECR Images') {
            steps {
                script {

                    echo "🔍 Verifying frontend and backend images in ECR..."

                    sh """
                        aws ecr describe-images \
                            --repository-name ${FRONTEND_ECR_REPOSITORY} \
                            --image-ids imageTag=${FRONTEND_TAG} \
                            --region ${AWS_REGION} \
                            --query 'imageDetails[0].imageTags' \
                            --output text
                    """

                    sh """
                        aws ecr describe-images \
                            --repository-name ${BACKEND_ECR_REPOSITORY} \
                            --image-ids imageTag=${BACKEND_TAG} \
                            --region ${AWS_REGION} \
                            --query 'imageDetails[0].imageTags' \
                            --output text
                    """

                    echo "✅ Both required ECR images are available."

                    echo "Frontend image: ${FRONTEND_IMAGE}"
                    echo "Backend image : ${BACKEND_IMAGE}"
                }
            }
        }


        stage('Generate Frontend Task Definition') {

            steps {

                script {

                    echo "📝 Generating frontend ECS task definition..."

                    // ==========================================
                    // Frontend Task Definition Variables
                    // ==========================================

                    env.FRONTEND_TASK_FAMILY = "itsoftlab-360-${PROJECT_NAME}-frontend"

                    env.FRONTEND_CONTAINER_NAME = "${PROJECT_NAME}-frontend"

                    env.FRONTEND_CONTAINER_PORT = "80"

                    env.FRONTEND_EXECUTION_ROLE = "arn:aws:iam::${AWS_ACCOUNT_ID}:role/ecsTaskExecutionRole"

                    env.FRONTEND_LOG_GROUP =  "/ecs/${FRONTEND_TASK_FAMILY}"


                    // ==========================================
                    // S3 Environment File ARN
                    // ==========================================

                    env.FRONTEND_ENV_FILE_ARN = "arn:aws:s3:::its360-secure-env-file/${FRONTEND_SECRET_KEY}"


                    echo "📦 Task Family      : ${FRONTEND_TASK_FAMILY}"
                    echo "📦 Container        : ${FRONTEND_CONTAINER_NAME}"
                    echo "📦 Image            : ${FRONTEND_IMAGE}"
                    echo "📦 Container Port   : ${FRONTEND_CONTAINER_PORT}"
                    echo "🔐 Environment File : ${FRONTEND_ENV_FILE_ARN}"
                    echo "📋 Log Group        : ${FRONTEND_LOG_GROUP}"


                    // ==========================================
                    // Generate Task Definition JSON
                    // ==========================================

                    sh """
                        cat > frontend-task-definition.json <<EOF
                    {
                        "family": "${FRONTEND_TASK_FAMILY}",

                        "executionRoleArn": "${FRONTEND_EXECUTION_ROLE}",

                        "networkMode": "awsvpc",

                        "requiresCompatibilities": [
                            "FARGATE"
                        ],

                        "cpu": "1024",

                        "memory": "3072",

                        "runtimePlatform": {
                            "cpuArchitecture": "X86_64",
                            "operatingSystemFamily": "LINUX"
                        },

                        "containerDefinitions": [
                            {
                                "name": "${FRONTEND_CONTAINER_NAME}",

                                "image": "${FRONTEND_IMAGE}",

                                "cpu": 0,

                                "portMappings": [
                                    {
                                        "containerPort": ${FRONTEND_CONTAINER_PORT},
                                        "hostPort": 80,
                                        "protocol": "tcp",
                                        "name": "${FRONTEND_CONTAINER_NAME}-80-tcp",
                                        "appProtocol": "http"
                                    }
                                ],

                                "essential": true,

                                "environment": [],

                                "environmentFiles": [
                                    {
                                        "value": "${FRONTEND_ENV_FILE_ARN}",
                                        "type": "s3"
                                    }
                                ],

                                "mountPoints": [],

                                "volumesFrom": [],

                                "ulimits": [],

                                "logConfiguration": {
                                    "logDriver": "awslogs",

                                    "options": {
                                        "awslogs-group": "${FRONTEND_LOG_GROUP}",
                                        "awslogs-create-group": "true",
                                        "awslogs-region": "${AWS_REGION}",
                                        "awslogs-stream-prefix": "ecs"
                                    }
                                },

                                "healthCheck": {
                                    "command": [
                                        "CMD-SHELL",
                                        "curl -f http://localhost/ || exit 1"
                                    ],

                                    "interval": 30,
                                    "timeout": 5,
                                    "retries": 3,
                                    "startPeriod": 10
                                }
                            }
                        ]
                    }
                    EOF
                                """


                    // ==========================================
                    // Validate Generated JSON
                    // ==========================================

                    echo "🔍 Validating generated task definition..."

                    sh """
                        jq empty frontend-task-definition.json
                    """

                    echo "✅ Frontend task definition JSON is valid."


                    // ==========================================
                    // Display Safe Configuration
                    // ==========================================

                    echo ""
                    echo "=========================================="
                    echo "✅ FRONTEND TASK DEFINITION GENERATED"
                    echo "=========================================="

                    sh """
                        jq '{
                            family,
                            executionRoleArn,
                            networkMode,
                            requiresCompatibilities,
                            cpu,
                            memory,
                            runtimePlatform,
                            containerDefinitions: [
                                {
                                    name: .containerDefinitions[0].name,
                                    image: .containerDefinitions[0].image,
                                    portMappings: .containerDefinitions[0].portMappings,
                                    environmentFiles: .containerDefinitions[0].environmentFiles,
                                    healthCheck: .containerDefinitions[0].healthCheck
                                }
                            ]
                        }' frontend-task-definition.json
                    """

                    echo "=========================================="
                }
            }
        }

        stage('Generate Backend Task Definition') {

            steps {

                script {

                    echo "📝 Generating backend ECS task definition..."

                    // ==========================================
                    // Backend Task Definition Variables
                    // ==========================================

                    env.BACKEND_TASK_FAMILY = "itsoftlab-360-${PROJECT_NAME}-backend"

                    env.BACKEND_CONTAINER_NAME = "${PROJECT_NAME}-backend"

                    env.BACKEND_CONTAINER_PORT = "5000"

                    env.BACKEND_EXECUTION_ROLE = "arn:aws:iam::${AWS_ACCOUNT_ID}:role/ecsTaskExecutionRole"

                    env.BACKEND_LOG_GROUP = "/ecs/${BACKEND_TASK_FAMILY}"


                    // ==========================================
                    // S3 Environment File ARN
                    // ==========================================

                    env.BACKEND_ENV_FILE_ARN =  "arn:aws:s3:::its360-secure-env-file/${BACKEND_SECRET_KEY}"


                    // ==========================================
                    // Display Configuration
                    // ==========================================

                    echo "📦 Task Family       : ${BACKEND_TASK_FAMILY}"
                    echo "📦 Container         : ${BACKEND_CONTAINER_NAME}"
                    echo "📦 Image             : ${BACKEND_IMAGE}"
                    echo "📦 Container Port    : ${BACKEND_CONTAINER_PORT}"
                    echo "🔐 Environment File  : ${BACKEND_ENV_FILE_ARN}"
                    echo "📋 Log Group         : ${BACKEND_LOG_GROUP}"


                    // ==========================================
                    // Generate Backend Task Definition JSON
                    // ==========================================

                    sh """
                        cat > backend-task-definition.json <<EOF

                        {
                            "family": "${BACKEND_TASK_FAMILY}",

                            "executionRoleArn": "${BACKEND_EXECUTION_ROLE}",

                            "networkMode": "awsvpc",

                            "requiresCompatibilities": [
                                "FARGATE"
                            ],

                            "cpu": "1024",

                            "memory": "3072",

                            "runtimePlatform": {
                                "cpuArchitecture": "X86_64",
                                "operatingSystemFamily": "LINUX"
                            },

                            "containerDefinitions": [
                                {
                                    "name": "${BACKEND_CONTAINER_NAME}",

                                    "image": "${BACKEND_IMAGE}",

                                    "cpu": 0,

                                    "portMappings": [
                                        {
                                            "containerPort": "${BACKEND_CONTAINER_PORT}",
                                            "hostPort": 5000,
                                            "protocol": "tcp",
                                            "name": "${BACKEND_CONTAINER_NAME}-5000-tcp",
                                            "appProtocol": "http"
                                        }
                                    ],

                                    "essential": true,

                                    "environment": [],

                                    "environmentFiles": [
                                        {
                                            "value": "${BACKEND_ENV_FILE_ARN}",
                                            "type": "s3"
                                        }
                                    ],

                                    "mountPoints": [],

                                    "volumesFrom": [],

                                    "ulimits": [],

                                    "logConfiguration": {
                                        "logDriver": "awslogs",

                                        "options": {
                                            "awslogs-group": "${BACKEND_LOG_GROUP}",
                                            "awslogs-create-group": "true",
                                            "awslogs-region": "${AWS_REGION}",
                                            "awslogs-stream-prefix": "ecs"
                                        }
                                    },

                                    "healthCheck": {
                                        "command": [
                                            "CMD-SHELL",
                                            "curl --fail --silent http://127.0.0.1:5000/health > /dev/null || exit 1"
                                        ],
                                        "interval": 30,
                                        "timeout": 5,
                                        "retries": 3,
                                        "startPeriod": 60
                                    }
                                }
                            ]
                        }

                        EOF
                    """


                    // ==========================================
                    // Validate Generated JSON
                    // ==========================================

                    echo "🔍 Validating generated backend task definition..."

                    sh """
                        jq empty backend-task-definition.json
                    """

                    echo "✅ Backend task definition JSON is valid."


                    // ==========================================
                    // Display Safe Configuration
                    // ==========================================

                    echo ""
                    echo "=========================================="
                    echo "✅ BACKEND TASK DEFINITION GENERATED"
                    echo "=========================================="

                    sh """
                        jq '{
                            family,
                            executionRoleArn,
                            networkMode,
                            requiresCompatibilities,
                            cpu,
                            memory,
                            runtimePlatform,
                            containerDefinitions: [
                                {
                                    name: .containerDefinitions[0].name,
                                    image: .containerDefinitions[0].image,
                                    portMappings: .containerDefinitions[0].portMappings,
                                    environmentFiles: .containerDefinitions[0].environmentFiles,
                                    healthCheck: .containerDefinitions[0].healthCheck
                                }
                            ]
                        }' backend-task-definition.json
                    """

                    echo "=========================================="
                }
            }
        }

        stage('Register Task Definitions') {

            steps {

                script {

                    echo "📋 Registering ECS task definitions..."

                    // ==========================================
                    // Register Frontend Task Definition
                    // ==========================================

                    echo "🚀 Registering frontend task definition..."

                    def frontendTaskDefinitionArn = sh(
                        script: """
                            aws ecs register-task-definition \
                                --cli-input-json file://frontend-task-definition.json \
                                --region ${AWS_REGION} \
                                --query 'taskDefinition.taskDefinitionArn' \
                                --output text
                        """,
                        returnStdout: true
                    ).trim()

                    if (!frontendTaskDefinitionArn) {
                        error "❌ Failed to register frontend task definition."
                    }

                    env.FRONTEND_TASK_DEFINITION_ARN = frontendTaskDefinitionArn

                    echo "✅ Frontend task definition registered:"
                    echo "${env.FRONTEND_TASK_DEFINITION_ARN}"


                    // ==========================================
                    // Register Backend Task Definition
                    // ==========================================

                    echo "🚀 Registering backend task definition..."

                    def backendTaskDefinitionArn = sh(
                        script: """
                            aws ecs register-task-definition \
                                --cli-input-json file://backend-task-definition.json \
                                --region ${AWS_REGION} \
                                --query 'taskDefinition.taskDefinitionArn' \
                                --output text
                        """,
                        returnStdout: true
                    ).trim()

                    if (!backendTaskDefinitionArn) {
                        error "❌ Failed to register backend task definition."
                    }

                    env.BACKEND_TASK_DEFINITION_ARN = backendTaskDefinitionArn

                    echo "✅ Backend task definition registered:"
                    echo "${env.BACKEND_TASK_DEFINITION_ARN}"


                    // ==========================================
                    // Summary
                    // ==========================================

                    echo ""
                    echo "=========================================="
                    echo "✅ TASK DEFINITIONS REGISTERED"
                    echo "=========================================="
                    echo "Frontend:"
                    echo "${env.FRONTEND_TASK_DEFINITION_ARN}"
                    echo ""
                    echo "Backend:"
                    echo "${env.BACKEND_TASK_DEFINITION_ARN}"
                    echo "=========================================="
                }
            }
        }


        stage('Create/Update ECS Services') {

            steps {

                script {

                    echo "🚀 Starting ECS infrastructure and service deployment..."


                    // =========================================================
                    // ECS CONFIGURATION
                    // =========================================================

                    env.ECS_CLUSTER = "its360-cluster"

                    env.FRONTEND_SERVICE = "${PROJECT_NAME}-frontend-service"
                    env.BACKEND_SERVICE = "${PROJECT_NAME}-backend-service"

                    env.FRONTEND_LOAD_BALANCER = "${PROJECT_NAME}-frontend-lb"
                    env.BACKEND_LOAD_BALANCER = "${PROJECT_NAME}-backend-lb"

                    env.FRONTEND_TARGET_GROUP = "${PROJECT_NAME}-frontend-tg"
                    env.BACKEND_TARGET_GROUP = "${PROJECT_NAME}-backend-tg"

                    env.FRONTEND_CONTAINER_NAME = "${PROJECT_NAME}-frontend"
                    env.BACKEND_CONTAINER_NAME = "${PROJECT_NAME}-backend"

                    env.FRONTEND_CONTAINER_PORT = "80"
                    env.BACKEND_CONTAINER_PORT = "5000"


                    // =========================================================
                    // NETWORK CONFIGURATION
                    // =========================================================

                    env.VPC_ID = "vpc-08bb09e9a7c10b095default"

                    env.SUBNET_1 = "subnet-06fe5b3d8e95ad780"
                    env.SUBNET_2 = "subnet-0cf9fd6f508540589"

                    env.SECURITY_GROUP_1 = "sg-05e021221abdb240e"
                    env.SECURITY_GROUP_2 = "sg-02f964fc431f9b80f"


                    // =========================================================
                    // LOAD BALANCER CONFIGURATION
                    // =========================================================

                    env.FRONTEND_LB_PORT = "80"
                    env.BACKEND_LB_PORT = "5000"

                    env.FRONTEND_HEALTH_PATH = "/"
                    env.BACKEND_HEALTH_PATH = "/health"


                    // =========================================================
                    // CHECK ECS CLUSTER
                    // =========================================================

                    echo "🔍 Checking ECS cluster..."

                    def clusterStatus = sh(
                        script: """
                            aws ecs describe-clusters \
                                --clusters ${ECS_CLUSTER} \
                                --region ${AWS_REGION} \
                                --query 'clusters[0].status' \
                                --output text
                        """,
                        returnStdout: true
                    ).trim()

                    if (clusterStatus != "ACTIVE") {

                        error """
                        ❌ ECS cluster '${ECS_CLUSTER}' is not ACTIVE.

                        Current status: ${clusterStatus}
                        """
                    }

                    echo "✅ ECS cluster is ACTIVE."


                    // =========================================================
                    // FRONTEND LOAD BALANCER
                    // =========================================================

                    echo ""
                    echo "=========================================="
                    echo "🌐 FRONTEND LOAD BALANCER"
                    echo "=========================================="


                    def frontendLoadBalancerArn = sh(
                        script: """
                            aws elbv2 describe-load-balancers \
                                --names ${FRONTEND_LOAD_BALANCER} \
                                --region ${AWS_REGION} \
                                --query 'LoadBalancers[0].LoadBalancerArn' \
                                --output text 2>/dev/null || true
                        """,
                        returnStdout: true
                    ).trim()


                    if (!frontendLoadBalancerArn ||  frontendLoadBalancerArn == "None") {

                        echo "📦 Frontend load balancer does not exist."
                        echo "🚀 Creating ${FRONTEND_LOAD_BALANCER}..."

                        frontendLoadBalancerArn = sh(
                            script: """
                                aws elbv2 create-load-balancer \
                                    --name ${FRONTEND_LOAD_BALANCER} \
                                    --type application \
                                    --scheme internet-facing \
                                    --subnets ${SUBNET_1} ${SUBNET_2} \
                                    --security-groups ${SECURITY_GROUP_1} ${SECURITY_GROUP_2} \
                                    --region ${AWS_REGION} \
                                    --query 'LoadBalancers[0].LoadBalancerArn' \
                                    --output text
                            """,
                            returnStdout: true
                        ).trim()

                        echo "✅ Frontend load balancer created."

                    } else {

                        echo "✅ Frontend load balancer already exists."
                        echo "Using existing load balancer."
                    }

                    env.FRONTEND_LOAD_BALANCER_ARN = frontendLoadBalancerArn

                    echo "Frontend ALB ARN:"
                    echo "${FRONTEND_LOAD_BALANCER_ARN}"


                    // =========================================================
                    // BACKEND LOAD BALANCER
                    // =========================================================

                    echo ""
                    echo "=========================================="
                    echo "⚙️ BACKEND LOAD BALANCER"
                    echo "=========================================="


                    def backendLoadBalancerArn = sh(
                        script: """
                            aws elbv2 describe-load-balancers \
                                --names ${BACKEND_LOAD_BALANCER} \
                                --region ${AWS_REGION} \
                                --query 'LoadBalancers[0].LoadBalancerArn' \
                                --output text 2>/dev/null || true
                        """,
                        returnStdout: true
                    ).trim()


                    if (!backendLoadBalancerArn ||  backendLoadBalancerArn == "None") {

                        echo "📦 Backend load balancer does not exist."
                        echo "🚀 Creating ${BACKEND_LOAD_BALANCER}..."

                        backendLoadBalancerArn = sh(
                            script: """
                                aws elbv2 create-load-balancer \
                                    --name ${BACKEND_LOAD_BALANCER} \
                                    --type application \
                                    --scheme internet-facing \
                                    --subnets ${SUBNET_1} ${SUBNET_2} \
                                    --security-groups ${SECURITY_GROUP_1} ${SECURITY_GROUP_2} \
                                    --region ${AWS_REGION} \
                                    --query 'LoadBalancers[0].LoadBalancerArn' \
                                    --output text
                            """,
                            returnStdout: true
                        ).trim()

                        echo "✅ Backend load balancer created."

                    } else {

                        echo "✅ Backend load balancer already exists."
                        echo "Using existing load balancer."
                    }

                    env.BACKEND_LOAD_BALANCER_ARN = backendLoadBalancerArn

                    echo "Backend ALB ARN:"
                    echo "${BACKEND_LOAD_BALANCER_ARN}"


                    // =========================================================
                    // FRONTEND TARGET GROUP
                    // =========================================================

                    echo ""
                    echo "=========================================="
                    echo "🎯 FRONTEND TARGET GROUP"
                    echo "=========================================="


                    def frontendTargetGroupArn = sh(
                        script: """
                            aws elbv2 describe-target-groups \
                                --names ${FRONTEND_TARGET_GROUP} \
                                --region ${AWS_REGION} \
                                --query 'TargetGroups[0].TargetGroupArn' \
                                --output text 2>/dev/null || true
                        """,
                        returnStdout: true
                    ).trim()


                    if (!frontendTargetGroupArn || frontendTargetGroupArn == "None") {

                        echo "📦 Frontend target group does not exist."
                        echo "🚀 Creating ${FRONTEND_TARGET_GROUP}..."

                        frontendTargetGroupArn = sh(
                            script: """
                                aws elbv2 create-target-group \
                                    --name ${FRONTEND_TARGET_GROUP} \
                                    --protocol HTTP \
                                    --port 80 \
                                    --target-type ip \
                                    --vpc-id ${VPC_ID} \
                                    --health-check-protocol HTTP \
                                    --health-check-path ${FRONTEND_HEALTH_PATH} \
                                    --health-check-port traffic-port \
                                    --health-check-interval-seconds 30 \
                                    --health-check-timeout-seconds 5 \
                                    --healthy-threshold-count 2 \
                                    --unhealthy-threshold-count 3 \
                                    --region ${AWS_REGION} \
                                    --query 'TargetGroups[0].TargetGroupArn' \
                                    --output text
                            """,
                            returnStdout: true
                        ).trim()

                        echo "✅ Frontend target group created."

                    } else {

                        echo "✅ Frontend target group already exists."
                        echo "Using existing target group."
                    }

                    env.FRONTEND_TARGET_GROUP_ARN = frontendTargetGroupArn

                    echo "Frontend Target Group ARN:"
                    echo "${FRONTEND_TARGET_GROUP_ARN}"


                    // =========================================================
                    // BACKEND TARGET GROUP
                    // =========================================================

                    echo ""
                    echo "=========================================="
                    echo "🎯 BACKEND TARGET GROUP"
                    echo "=========================================="


                    def backendTargetGroupArn = sh(
                        script: """
                            aws elbv2 describe-target-groups \
                                --names ${BACKEND_TARGET_GROUP} \
                                --region ${AWS_REGION} \
                                --query 'TargetGroups[0].TargetGroupArn' \
                                --output text 2>/dev/null || true
                        """,
                        returnStdout: true
                    ).trim()


                    if (!backendTargetGroupArn ||
                        backendTargetGroupArn == "None") {

                        echo "📦 Backend target group does not exist."
                        echo "🚀 Creating ${BACKEND_TARGET_GROUP}..."

                        backendTargetGroupArn = sh(
                            script: """
                                aws elbv2 create-target-group \
                                    --name ${BACKEND_TARGET_GROUP} \
                                    --protocol HTTP \
                                    --port 5000 \
                                    --target-type ip \
                                    --vpc-id ${VPC_ID} \
                                    --health-check-protocol HTTP \
                                    --health-check-path ${BACKEND_HEALTH_PATH} \
                                    --health-check-port traffic-port \
                                    --health-check-interval-seconds 30 \
                                    --health-check-timeout-seconds 5 \
                                    --healthy-threshold-count 2 \
                                    --unhealthy-threshold-count 3 \
                                    --region ${AWS_REGION} \
                                    --query 'TargetGroups[0].TargetGroupArn' \
                                    --output text
                            """,
                            returnStdout: true
                        ).trim()

                        echo "✅ Backend target group created."

                    } else {

                        echo "✅ Backend target group already exists."
                        echo "Using existing target group."
                    }

                    env.BACKEND_TARGET_GROUP_ARN = backendTargetGroupArn

                    echo "Backend Target Group ARN:"
                    echo "${BACKEND_TARGET_GROUP_ARN}"


                    // =========================================================
                    // FRONTEND LISTENER
                    // =========================================================

                    echo ""
                    echo "=========================================="
                    echo "🔊 FRONTEND LISTENER"
                    echo "=========================================="


                    def frontendListenerArn = sh(
                        script: """
                            aws elbv2 describe-listeners \
                                --load-balancer-arn ${FRONTEND_LOAD_BALANCER_ARN} \
                                --region ${AWS_REGION} \
                                --query 'Listeners[?Port==\\`80\\`].ListenerArn | [0]' \
                                --output text 2>/dev/null || true
                        """,
                        returnStdout: true
                    ).trim()


                    if (!frontendListenerArn || frontendListenerArn == "None") {

                        echo "📦 Frontend listener does not exist."
                        echo "🚀 Creating HTTP :80 listener..."

                        frontendListenerArn = sh(
                            script: """
                                aws elbv2 create-listener \
                                    --load-balancer-arn ${FRONTEND_LOAD_BALANCER_ARN} \
                                    --protocol HTTP \
                                    --port 80 \
                                    --default-actions Type=forward,TargetGroupArn=${FRONTEND_TARGET_GROUP_ARN} \
                                    --region ${AWS_REGION} \
                                    --query 'Listeners[0].ListenerArn' \
                                    --output text
                            """,
                            returnStdout: true
                        ).trim()

                        echo "✅ Frontend listener created."

                    } else {

                        echo "✅ Frontend listener already exists."
                        echo "🔄 Ensuring listener forwards to frontend target group..."

                        sh """
                            aws elbv2 modify-listener \
                                --listener-arn ${frontendListenerArn} \
                                --default-actions Type=forward,TargetGroupArn=${FRONTEND_TARGET_GROUP_ARN} \
                                --region ${AWS_REGION}
                        """

                        echo "✅ Frontend listener configuration verified."
                    }

                    env.FRONTEND_LISTENER_ARN = frontendListenerArn


                    // =========================================================
                    // BACKEND LISTENER
                    // =========================================================

                    echo ""
                    echo "=========================================="
                    echo "🔊 BACKEND LISTENER"
                    echo "=========================================="


                    def backendListenerArn = sh(
                        script: """
                            aws elbv2 describe-listeners \
                                --load-balancer-arn ${BACKEND_LOAD_BALANCER_ARN} \
                                --region ${AWS_REGION} \
                                --query 'Listeners[?Port==\\`5000\\`].ListenerArn | [0]' \
                                --output text 2>/dev/null || true
                        """,
                        returnStdout: true
                    ).trim()


                    if (!backendListenerArn ||
                        backendListenerArn == "None") {

                        echo "📦 Backend listener does not exist."
                        echo "🚀 Creating HTTP :5000 listener..."

                        backendListenerArn = sh(
                            script: """
                                aws elbv2 create-listener \
                                    --load-balancer-arn ${BACKEND_LOAD_BALANCER_ARN} \
                                    --protocol HTTP \
                                    --port 5000 \
                                    --default-actions Type=forward,TargetGroupArn=${BACKEND_TARGET_GROUP_ARN} \
                                    --region ${AWS_REGION} \
                                    --query 'Listeners[0].ListenerArn' \
                                    --output text
                            """,
                            returnStdout: true
                        ).trim()

                        echo "✅ Backend listener created."

                    } else {

                        echo "✅ Backend listener already exists."
                        echo "🔄 Ensuring listener forwards to backend target group..."

                        sh """
                            aws elbv2 modify-listener \
                                --listener-arn ${backendListenerArn} \
                                --default-actions Type=forward,TargetGroupArn=${BACKEND_TARGET_GROUP_ARN} \
                                --region ${AWS_REGION}
                        """

                        echo "✅ Backend listener configuration verified."
                    }

                    env.BACKEND_LISTENER_ARN = backendListenerArn


                    // =========================================================
                    // FRONTEND ECS SERVICE
                    // =========================================================

                    echo ""
                    echo "=========================================="
                    echo "🌐 FRONTEND ECS SERVICE"
                    echo "=========================================="


                    def frontendServiceArn = sh(
                        script: """
                            aws ecs describe-services \
                                --cluster ${ECS_CLUSTER} \
                                --services ${FRONTEND_SERVICE} \
                                --region ${AWS_REGION} \
                                --query 'services[0].serviceArn' \
                                --output text 2>/dev/null || true
                        """,
                        returnStdout: true
                    ).trim()


                    if (!frontendServiceArn ||
                        frontendServiceArn == "None") {

                        echo "📦 Frontend ECS service does not exist."
                        echo "🚀 Creating ${FRONTEND_SERVICE}..."

                        sh """
                            aws ecs create-service \
                                --cluster ${ECS_CLUSTER} \
                                --service-name ${FRONTEND_SERVICE} \
                                --task-definition ${FRONTEND_TASK_DEFINITION_ARN} \
                                --desired-count 1 \
                                --launch-type FARGATE \
                                --platform-version 1.4.0 \
                                --network-configuration "awsvpcConfiguration={
                                    subnets=[${SUBNET_1},${SUBNET_2}],
                                    securityGroups=[${SECURITY_GROUP_1},${SECURITY_GROUP_2}],
                                    assignPublicIp=ENABLED
                                }" \
                                --load-balancers "targetGroupArn=${FRONTEND_TARGET_GROUP_ARN},containerName=${FRONTEND_CONTAINER_NAME},containerPort=80" \
                                --region ${AWS_REGION}
                        """

                        echo "✅ Frontend ECS service created."

                    } else {

                        echo "✅ Frontend ECS service already exists."
                        echo "🔄 Updating frontend ECS service..."

                        sh """
                            aws ecs update-service \
                                --cluster ${ECS_CLUSTER} \
                                --service ${FRONTEND_SERVICE} \
                                --task-definition ${FRONTEND_TASK_DEFINITION_ARN} \
                                --load-balancers "targetGroupArn=${FRONTEND_TARGET_GROUP_ARN},containerName=${FRONTEND_CONTAINER_NAME},containerPort=80" \
                                --region ${AWS_REGION}
                        """

                        echo "✅ Frontend ECS service updated."
                    }


                    // =========================================================
                    // BACKEND ECS SERVICE
                    // =========================================================

                    echo ""
                    echo "=========================================="
                    echo "⚙️ BACKEND ECS SERVICE"
                    echo "=========================================="


                    def backendServiceArn = sh(
                        script: """
                            aws ecs describe-services \
                                --cluster ${ECS_CLUSTER} \
                                --services ${BACKEND_SERVICE} \
                                --region ${AWS_REGION} \
                                --query 'services[0].serviceArn' \
                                --output text 2>/dev/null || true
                        """,
                        returnStdout: true
                    ).trim()


                    if (!backendServiceArn || backendServiceArn == "None") {

                        echo "📦 Backend ECS service does not exist."
                        echo "🚀 Creating ${BACKEND_SERVICE}..."

                        sh """
                            aws ecs create-service \
                                --cluster ${ECS_CLUSTER} \
                                --service-name ${BACKEND_SERVICE} \
                                --task-definition ${BACKEND_TASK_DEFINITION_ARN} \
                                --desired-count 1 \
                                --launch-type FARGATE \
                                --platform-version 1.4.0 \
                                --network-configuration "awsvpcConfiguration={
                                    subnets=[${SUBNET_1},${SUBNET_2}],
                                    securityGroups=[${SECURITY_GROUP_1},${SECURITY_GROUP_2}],
                                    assignPublicIp=ENABLED
                                }" \
                                --load-balancers "targetGroupArn=${BACKEND_TARGET_GROUP_ARN},containerName=${BACKEND_CONTAINER_NAME},containerPort=5000" \
                                --region ${AWS_REGION}
                        """

                        echo "✅ Backend ECS service created."

                    } else {

                        echo "✅ Backend ECS service already exists."
                        echo "🔄 Updating backend ECS service..."

                        sh """
                            aws ecs update-service \
                                --cluster ${ECS_CLUSTER} \
                                --service ${BACKEND_SERVICE} \
                                --task-definition ${BACKEND_TASK_DEFINITION_ARN} \
                                --load-balancers "targetGroupArn=${BACKEND_TARGET_GROUP_ARN},containerName=${BACKEND_CONTAINER_NAME},containerPort=5000" \
                                --region ${AWS_REGION}
                        """

                        echo "✅ Backend ECS service updated."
                    }


                    // =========================================================
                    // FINAL SUMMARY
                    // =========================================================

                    echo ""
                    echo "=========================================="
                    echo "✅ ECS DEPLOYMENT CONFIGURED"
                    echo "=========================================="

                    echo ""
                    echo "Cluster:"
                    echo "${ECS_CLUSTER}"

                    echo ""
                    echo "Frontend:"
                    echo "  Load Balancer : ${FRONTEND_LOAD_BALANCER}"
                    echo "  Target Group  : ${FRONTEND_TARGET_GROUP}"
                    echo "  Listener      : HTTP :80"
                    echo "  Service       : ${FRONTEND_SERVICE}"
                    echo "  Task Definition:"
                    echo "  ${FRONTEND_TASK_DEFINITION_ARN}"

                    echo ""
                    echo "Backend:"
                    echo "  Load Balancer : ${BACKEND_LOAD_BALANCER}"
                    echo "  Target Group  : ${BACKEND_TARGET_GROUP}"
                    echo "  Listener      : HTTP :5000"
                    echo "  Service       : ${BACKEND_SERVICE}"
                    echo "  Task Definition:"
                    echo "  ${BACKEND_TASK_DEFINITION_ARN}"

                    echo ""
                    echo "=========================================="
                }
            }
        }


        stage('Wait for ECS Stability') {
            steps {
                script {

                    echo ""
                    echo "=========================================="
                    echo "⏳ WAITING FOR ECS SERVICES TO STABILIZE"
                    echo "=========================================="

                    // =========================================================
                    // VALIDATE REQUIRED VARIABLES
                    // =========================================================

                    if (!env.ECS_CLUSTER) {
                        error "❌ ECS_CLUSTER is not available."
                    }

                    if (!env.FRONTEND_SERVICE) {
                        error "❌ FRONTEND_SERVICE is not available."
                    }

                    if (!env.BACKEND_SERVICE) {
                        error "❌ BACKEND_SERVICE is not available."
                    }

                    echo "Cluster          : ${ECS_CLUSTER}"
                    echo "Frontend Service : ${FRONTEND_SERVICE}"
                    echo "Backend Service  : ${BACKEND_SERVICE}"

                    // =========================================================
                    // WAIT FOR FRONTEND SERVICE
                    // =========================================================

                    echo ""
                    echo "🌐 Waiting for frontend ECS service..."

                    sh """
                        aws ecs wait services-stable \
                            --cluster ${ECS_CLUSTER} \
                            --services ${FRONTEND_SERVICE} \
                            --region ${AWS_REGION}
                    """

                    echo "✅ Frontend ECS service is stable."

                    // =========================================================
                    // WAIT FOR BACKEND SERVICE
                    // =========================================================

                    echo ""
                    echo "⚙️ Waiting for backend ECS service..."

                    sh """
                        aws ecs wait services-stable \
                            --cluster ${ECS_CLUSTER} \
                            --services ${BACKEND_SERVICE} \
                            --region ${AWS_REGION}
                    """

                    echo "✅ Backend ECS service is stable."

                    // =========================================================
                    // VERIFY FINAL SERVICE STATUS
                    // =========================================================

                    echo ""
                    echo "🔍 Verifying final ECS service status..."

                    sh """
                        aws ecs describe-services \
                            --cluster ${ECS_CLUSTER} \
                            --services ${FRONTEND_SERVICE} ${BACKEND_SERVICE} \
                            --region ${AWS_REGION} \
                            --query 'services[].{
                                Service:serviceName,
                                Status:status,
                                Desired:desiredCount,
                                Running:runningCount,
                                Pending:pendingCount
                            }' \
                            --output table
                    """

                    echo ""
                    echo "=========================================="
                    echo "✅ ECS SERVICES ARE STABLE"
                    echo "=========================================="
                }
            }
        }

        stage('Health Check') {
            steps {
                script {

                    echo ""
                    echo "=========================================="
                    echo "🏥 ECS APPLICATION HEALTH CHECK"
                    echo "=========================================="

                    // =========================================================
                    // VALIDATE REQUIRED VARIABLES
                    // =========================================================

                    if (!env.ECS_CLUSTER) {
                        error "❌ ECS_CLUSTER is not available."
                    }

                    if (!env.FRONTEND_SERVICE) {
                        error "❌ FRONTEND_SERVICE is not available."
                    }

                    if (!env.BACKEND_SERVICE) {
                        error "❌ BACKEND_SERVICE is not available."
                    }

                    if (!env.FRONTEND_TARGET_GROUP_ARN) {
                        error "❌ FRONTEND_TARGET_GROUP_ARN is not available."
                    }

                    if (!env.BACKEND_TARGET_GROUP_ARN) {
                        error "❌ BACKEND_TARGET_GROUP_ARN is not available."
                    }

                    // =========================================================
                    // FRONTEND ECS SERVICE HEALTH
                    // =========================================================

                    echo ""
                    echo "🌐 Checking frontend ECS service health..."

                    def frontendHealth = sh(
                        script: """
                            aws ecs describe-services \
                                --cluster ${ECS_CLUSTER} \
                                --services ${FRONTEND_SERVICE} \
                                --region ${AWS_REGION} \
                                --query 'services[0].{
                                    Status:status,
                                    Desired:desiredCount,
                                    Running:runningCount,
                                    Pending:pendingCount,
                                    Deployments:deployments[0].rolloutState
                                }' \
                                --output json
                        """,
                        returnStdout: true
                    ).trim()

                    echo "Frontend ECS status:"
                    echo frontendHealth

                    // =========================================================
                    // BACKEND ECS SERVICE HEALTH
                    // =========================================================

                    echo ""
                    echo "⚙️ Checking backend ECS service health..."

                    def backendHealth = sh(
                        script: """
                            aws ecs describe-services \
                                --cluster ${ECS_CLUSTER} \
                                --services ${BACKEND_SERVICE} \
                                --region ${AWS_REGION} \
                                --query 'services[0].{
                                    Status:status,
                                    Desired:desiredCount,
                                    Running:runningCount,
                                    Pending:pendingCount,
                                    Deployments:deployments[0].rolloutState
                                }' \
                                --output json
                        """,
                        returnStdout: true
                    ).trim()

                    echo "Backend ECS status:"
                    echo backendHealth

                    // =========================================================
                    // CHECK ECS TASK HEALTH
                    // =========================================================

                    echo ""
                    echo "🔍 Checking frontend and backend task health..."

                    def frontendTaskHealth = sh(
                        script: """
                            aws ecs list-tasks \
                                --cluster ${ECS_CLUSTER} \
                                --service-name ${FRONTEND_SERVICE} \
                                --desired-status RUNNING \
                                --region ${AWS_REGION} \
                                --query 'taskArns' \
                                --output text
                        """,
                        returnStdout: true
                    ).trim()

                    if (!frontendTaskHealth || frontendTaskHealth == "None") {
                        error "❌ No RUNNING frontend ECS task found."
                    }

                    def backendTaskHealth = sh(
                        script: """
                            aws ecs list-tasks \
                                --cluster ${ECS_CLUSTER} \
                                --service-name ${BACKEND_SERVICE} \
                                --desired-status RUNNING \
                                --region ${AWS_REGION} \
                                --query 'taskArns' \
                                --output text
                        """,
                        returnStdout: true
                    ).trim()

                    if (!backendTaskHealth || backendTaskHealth == "None") {
                        error "❌ No RUNNING backend ECS task found."
                    }

                    echo "✅ Frontend has RUNNING task(s)."
                    echo "✅ Backend has RUNNING task(s)."

                    // =========================================================
                    // FRONTEND ALB TARGET HEALTH
                    // =========================================================

                    echo ""
                    echo "🌐 Checking frontend ALB target health..."

                    def frontendTargetHealth = sh(
                        script: """
                            aws elbv2 describe-target-health \
                                --target-group-arn ${FRONTEND_TARGET_GROUP_ARN} \
                                --region ${AWS_REGION} \
                                --query 'TargetHealthDescriptions[].TargetHealth.State' \
                                --output text
                        """,
                        returnStdout: true
                    ).trim()

                    echo "Frontend target health: ${frontendTargetHealth}"

                    if (!frontendTargetHealth) {
                        error "❌ Frontend target group has no registered targets."
                    }

                    def frontendUnhealthy = sh(
                        script: """
                            aws elbv2 describe-target-health \
                                --target-group-arn ${FRONTEND_TARGET_GROUP_ARN} \
                                --region ${AWS_REGION} \
                                --query 'TargetHealthDescriptions[?TargetHealth.State!=`healthy`].TargetHealth.State' \
                                --output text
                        """,
                        returnStdout: true
                    ).trim()

                    if (frontendUnhealthy) {
                        echo "❌ Frontend unhealthy targets detected:"
                        echo "${frontendUnhealthy}"
                        error "❌ Frontend ALB target health check failed."
                    }

                    echo "✅ Frontend ALB targets are healthy."

                    // =========================================================
                    // BACKEND ALB TARGET HEALTH
                    // =========================================================

                    echo ""
                    echo "⚙️ Checking backend ALB target health..."

                    def backendTargetHealth = sh(
                        script: """
                            aws elbv2 describe-target-health \
                                --target-group-arn ${BACKEND_TARGET_GROUP_ARN} \
                                --region ${AWS_REGION} \
                                --query 'TargetHealthDescriptions[].TargetHealth.State' \
                                --output text
                        """,
                        returnStdout: true
                    ).trim()

                    echo "Backend target health: ${backendTargetHealth}"

                    if (!backendTargetHealth) {
                        error "❌ Backend target group has no registered targets."
                    }

                    def backendUnhealthy = sh(
                        script: """
                            aws elbv2 describe-target-health \
                                --target-group-arn ${BACKEND_TARGET_GROUP_ARN} \
                                --region ${AWS_REGION} \
                                --query 'TargetHealthDescriptions[?TargetHealth.State!=`healthy`].TargetHealth.State' \
                                --output text
                        """,
                        returnStdout: true
                    ).trim()

                    if (backendUnhealthy) {
                        echo "❌ Backend unhealthy targets detected:"
                        echo "${backendUnhealthy}"
                        error "❌ Backend ALB target health check failed."
                    }

                    echo "✅ Backend ALB targets are healthy."

                    // =========================================================
                    // FINAL HEALTH SUMMARY
                    // =========================================================

                    echo ""
                    echo "=========================================="
                    echo "✅ HEALTH CHECK PASSED"
                    echo "=========================================="
                    echo ""
                    echo "Frontend ECS : HEALTHY"
                    echo "Frontend ALB : HEALTHY"
                    echo "Backend ECS  : HEALTHY"
                    echo "Backend ALB  : HEALTHY"
                    echo ""
                    echo "🚀 Application deployment is healthy."
                    echo "=========================================="
                }
            }
        }

    }
}